@model IEnumerable<Chat>

<div class="chat-body">
        @foreach(var chat in Model)
        {
            <a class="room-button" asp-action ="JoinChat" asp-controller ="Home" asp-route-Id = "@chat.Id" >@chat.Name</a>
        }
</div>